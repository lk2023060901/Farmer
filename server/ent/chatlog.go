// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/chatlog"
	"github.com/liukai/farmer/server/ent/user"
)

// ChatLog is the model entity for the ChatLog schema.
type ChatLog struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 会话双方（小ID）
	UserAID uuid.UUID `json:"user_a_id,omitempty"`
	// 会话双方（大ID）
	UserBID uuid.UUID `json:"user_b_id,omitempty"`
	// 本条发言方
	SpeakerUserID uuid.UUID `json:"speaker_user_id,omitempty"`
	// visit/trade/help/gift 等
	Scene string `json:"scene,omitempty"`
	// 对话内容
	Content string `json:"content,omitempty"`
	// IsLlmGenerated holds the value of the "is_llm_generated" field.
	IsLlmGenerated bool `json:"is_llm_generated,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ChatLogQuery when eager-loading is set.
	Edges        ChatLogEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ChatLogEdges holds the relations/edges for other nodes in the graph.
type ChatLogEdges struct {
	// UserA holds the value of the user_a edge.
	UserA *User `json:"user_a,omitempty"`
	// UserB holds the value of the user_b edge.
	UserB *User `json:"user_b,omitempty"`
	// Speaker holds the value of the speaker edge.
	Speaker *User `json:"speaker,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// UserAOrErr returns the UserA value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatLogEdges) UserAOrErr() (*User, error) {
	if e.UserA != nil {
		return e.UserA, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user_a"}
}

// UserBOrErr returns the UserB value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatLogEdges) UserBOrErr() (*User, error) {
	if e.UserB != nil {
		return e.UserB, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user_b"}
}

// SpeakerOrErr returns the Speaker value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ChatLogEdges) SpeakerOrErr() (*User, error) {
	if e.Speaker != nil {
		return e.Speaker, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "speaker"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ChatLog) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case chatlog.FieldIsLlmGenerated:
			values[i] = new(sql.NullBool)
		case chatlog.FieldScene, chatlog.FieldContent:
			values[i] = new(sql.NullString)
		case chatlog.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case chatlog.FieldID, chatlog.FieldUserAID, chatlog.FieldUserBID, chatlog.FieldSpeakerUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ChatLog fields.
func (_m *ChatLog) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case chatlog.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case chatlog.FieldUserAID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_a_id", values[i])
			} else if value != nil {
				_m.UserAID = *value
			}
		case chatlog.FieldUserBID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_b_id", values[i])
			} else if value != nil {
				_m.UserBID = *value
			}
		case chatlog.FieldSpeakerUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field speaker_user_id", values[i])
			} else if value != nil {
				_m.SpeakerUserID = *value
			}
		case chatlog.FieldScene:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field scene", values[i])
			} else if value.Valid {
				_m.Scene = value.String
			}
		case chatlog.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				_m.Content = value.String
			}
		case chatlog.FieldIsLlmGenerated:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_llm_generated", values[i])
			} else if value.Valid {
				_m.IsLlmGenerated = value.Bool
			}
		case chatlog.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ChatLog.
// This includes values selected through modifiers, order, etc.
func (_m *ChatLog) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUserA queries the "user_a" edge of the ChatLog entity.
func (_m *ChatLog) QueryUserA() *UserQuery {
	return NewChatLogClient(_m.config).QueryUserA(_m)
}

// QueryUserB queries the "user_b" edge of the ChatLog entity.
func (_m *ChatLog) QueryUserB() *UserQuery {
	return NewChatLogClient(_m.config).QueryUserB(_m)
}

// QuerySpeaker queries the "speaker" edge of the ChatLog entity.
func (_m *ChatLog) QuerySpeaker() *UserQuery {
	return NewChatLogClient(_m.config).QuerySpeaker(_m)
}

// Update returns a builder for updating this ChatLog.
// Note that you need to call ChatLog.Unwrap() before calling this method if this ChatLog
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ChatLog) Update() *ChatLogUpdateOne {
	return NewChatLogClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ChatLog entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ChatLog) Unwrap() *ChatLog {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ChatLog is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ChatLog) String() string {
	var builder strings.Builder
	builder.WriteString("ChatLog(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_a_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserAID))
	builder.WriteString(", ")
	builder.WriteString("user_b_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserBID))
	builder.WriteString(", ")
	builder.WriteString("speaker_user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SpeakerUserID))
	builder.WriteString(", ")
	builder.WriteString("scene=")
	builder.WriteString(_m.Scene)
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(_m.Content)
	builder.WriteString(", ")
	builder.WriteString("is_llm_generated=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsLlmGenerated))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ChatLogs is a parsable slice of ChatLog.
type ChatLogs []*ChatLog
