// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/animal"
	"github.com/liukai/farmer/server/ent/farm"
)

// Animal is the model entity for the Animal schema.
type Animal struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// FarmID holds the value of the "farm_id" field.
	FarmID uuid.UUID `json:"farm_id,omitempty"`
	// chicken/cow/sheep/bee/rabbit
	Type string `json:"type,omitempty"`
	// 玩家自定义名字
	Name *string `json:"name,omitempty"`
	// Mood holds the value of the "mood" field.
	Mood int `json:"mood,omitempty"`
	// Health holds the value of the "health" field.
	Health int `json:"health,omitempty"`
	// LastFedAt holds the value of the "last_fed_at" field.
	LastFedAt *time.Time `json:"last_fed_at,omitempty"`
	// LastCleanedAt holds the value of the "last_cleaned_at" field.
	LastCleanedAt *time.Time `json:"last_cleaned_at,omitempty"`
	// 最后产出时间
	LastProductAt *time.Time `json:"last_product_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AnimalQuery when eager-loading is set.
	Edges        AnimalEdges `json:"edges"`
	selectValues sql.SelectValues
}

// AnimalEdges holds the relations/edges for other nodes in the graph.
type AnimalEdges struct {
	// Farm holds the value of the farm edge.
	Farm *Farm `json:"farm,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// FarmOrErr returns the Farm value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e AnimalEdges) FarmOrErr() (*Farm, error) {
	if e.Farm != nil {
		return e.Farm, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: farm.Label}
	}
	return nil, &NotLoadedError{edge: "farm"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Animal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case animal.FieldMood, animal.FieldHealth:
			values[i] = new(sql.NullInt64)
		case animal.FieldType, animal.FieldName:
			values[i] = new(sql.NullString)
		case animal.FieldCreatedAt, animal.FieldUpdatedAt, animal.FieldLastFedAt, animal.FieldLastCleanedAt, animal.FieldLastProductAt:
			values[i] = new(sql.NullTime)
		case animal.FieldID, animal.FieldFarmID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Animal fields.
func (_m *Animal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case animal.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case animal.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case animal.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case animal.FieldFarmID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field farm_id", values[i])
			} else if value != nil {
				_m.FarmID = *value
			}
		case animal.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				_m.Type = value.String
			}
		case animal.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = new(string)
				*_m.Name = value.String
			}
		case animal.FieldMood:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field mood", values[i])
			} else if value.Valid {
				_m.Mood = int(value.Int64)
			}
		case animal.FieldHealth:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field health", values[i])
			} else if value.Valid {
				_m.Health = int(value.Int64)
			}
		case animal.FieldLastFedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_fed_at", values[i])
			} else if value.Valid {
				_m.LastFedAt = new(time.Time)
				*_m.LastFedAt = value.Time
			}
		case animal.FieldLastCleanedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_cleaned_at", values[i])
			} else if value.Valid {
				_m.LastCleanedAt = new(time.Time)
				*_m.LastCleanedAt = value.Time
			}
		case animal.FieldLastProductAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_product_at", values[i])
			} else if value.Valid {
				_m.LastProductAt = new(time.Time)
				*_m.LastProductAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Animal.
// This includes values selected through modifiers, order, etc.
func (_m *Animal) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryFarm queries the "farm" edge of the Animal entity.
func (_m *Animal) QueryFarm() *FarmQuery {
	return NewAnimalClient(_m.config).QueryFarm(_m)
}

// Update returns a builder for updating this Animal.
// Note that you need to call Animal.Unwrap() before calling this method if this Animal
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Animal) Update() *AnimalUpdateOne {
	return NewAnimalClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Animal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Animal) Unwrap() *Animal {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Animal is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Animal) String() string {
	var builder strings.Builder
	builder.WriteString("Animal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("farm_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.FarmID))
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(_m.Type)
	builder.WriteString(", ")
	if v := _m.Name; v != nil {
		builder.WriteString("name=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("mood=")
	builder.WriteString(fmt.Sprintf("%v", _m.Mood))
	builder.WriteString(", ")
	builder.WriteString("health=")
	builder.WriteString(fmt.Sprintf("%v", _m.Health))
	builder.WriteString(", ")
	if v := _m.LastFedAt; v != nil {
		builder.WriteString("last_fed_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := _m.LastCleanedAt; v != nil {
		builder.WriteString("last_cleaned_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", ")
	if v := _m.LastProductAt; v != nil {
		builder.WriteString("last_product_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// Animals is a parsable slice of Animal.
type Animals []*Animal
