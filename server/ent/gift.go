// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/gift"
	"github.com/liukai/farmer/server/ent/user"
)

// Gift is the model entity for the Gift schema.
type Gift struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// SenderID holds the value of the "sender_id" field.
	SenderID uuid.UUID `json:"sender_id,omitempty"`
	// ReceiverID holds the value of the "receiver_id" field.
	ReceiverID uuid.UUID `json:"receiver_id,omitempty"`
	// ItemID holds the value of the "item_id" field.
	ItemID string `json:"item_id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity int `json:"quantity,omitempty"`
	// Message holds the value of the "message" field.
	Message *string `json:"message,omitempty"`
	// 好感度变化量
	AffinityGained int `json:"affinity_gained,omitempty"`
	// 是否 Agent 自动赠礼
	IsAgentAction bool `json:"is_agent_action,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the GiftQuery when eager-loading is set.
	Edges        GiftEdges `json:"edges"`
	selectValues sql.SelectValues
}

// GiftEdges holds the relations/edges for other nodes in the graph.
type GiftEdges struct {
	// Sender holds the value of the sender edge.
	Sender *User `json:"sender,omitempty"`
	// Receiver holds the value of the receiver edge.
	Receiver *User `json:"receiver,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// SenderOrErr returns the Sender value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GiftEdges) SenderOrErr() (*User, error) {
	if e.Sender != nil {
		return e.Sender, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "sender"}
}

// ReceiverOrErr returns the Receiver value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e GiftEdges) ReceiverOrErr() (*User, error) {
	if e.Receiver != nil {
		return e.Receiver, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "receiver"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Gift) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case gift.FieldIsAgentAction:
			values[i] = new(sql.NullBool)
		case gift.FieldQuantity, gift.FieldAffinityGained:
			values[i] = new(sql.NullInt64)
		case gift.FieldItemID, gift.FieldMessage:
			values[i] = new(sql.NullString)
		case gift.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case gift.FieldID, gift.FieldSenderID, gift.FieldReceiverID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Gift fields.
func (_m *Gift) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case gift.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case gift.FieldSenderID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field sender_id", values[i])
			} else if value != nil {
				_m.SenderID = *value
			}
		case gift.FieldReceiverID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field receiver_id", values[i])
			} else if value != nil {
				_m.ReceiverID = *value
			}
		case gift.FieldItemID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field item_id", values[i])
			} else if value.Valid {
				_m.ItemID = value.String
			}
		case gift.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				_m.Quantity = int(value.Int64)
			}
		case gift.FieldMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field message", values[i])
			} else if value.Valid {
				_m.Message = new(string)
				*_m.Message = value.String
			}
		case gift.FieldAffinityGained:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field affinity_gained", values[i])
			} else if value.Valid {
				_m.AffinityGained = int(value.Int64)
			}
		case gift.FieldIsAgentAction:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_agent_action", values[i])
			} else if value.Valid {
				_m.IsAgentAction = value.Bool
			}
		case gift.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Gift.
// This includes values selected through modifiers, order, etc.
func (_m *Gift) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySender queries the "sender" edge of the Gift entity.
func (_m *Gift) QuerySender() *UserQuery {
	return NewGiftClient(_m.config).QuerySender(_m)
}

// QueryReceiver queries the "receiver" edge of the Gift entity.
func (_m *Gift) QueryReceiver() *UserQuery {
	return NewGiftClient(_m.config).QueryReceiver(_m)
}

// Update returns a builder for updating this Gift.
// Note that you need to call Gift.Unwrap() before calling this method if this Gift
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Gift) Update() *GiftUpdateOne {
	return NewGiftClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Gift entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Gift) Unwrap() *Gift {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Gift is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Gift) String() string {
	var builder strings.Builder
	builder.WriteString("Gift(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("sender_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.SenderID))
	builder.WriteString(", ")
	builder.WriteString("receiver_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ReceiverID))
	builder.WriteString(", ")
	builder.WriteString("item_id=")
	builder.WriteString(_m.ItemID)
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	if v := _m.Message; v != nil {
		builder.WriteString("message=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("affinity_gained=")
	builder.WriteString(fmt.Sprintf("%v", _m.AffinityGained))
	builder.WriteString(", ")
	builder.WriteString("is_agent_action=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsAgentAction))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Gifts is a parsable slice of Gift.
type Gifts []*Gift
