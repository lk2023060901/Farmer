// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/farm"
	"github.com/liukai/farmer/server/ent/schema"
	"github.com/liukai/farmer/server/ent/user"
)

// Farm is the model entity for the Farm schema.
type Farm struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 农场主 user_id
	OwnerID uuid.UUID `json:"owner_id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Level holds the value of the "level" field.
	Level int `json:"level,omitempty"`
	// 村庄特产作物 ID 列表
	Specialty []string `json:"specialty,omitempty"`
	// 8×8 地块状态数组
	Plots []schema.PlotState `json:"plots,omitempty"`
	// 最后一次 Tick 时间
	LastTickAt time.Time `json:"last_tick_at,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FarmQuery when eager-loading is set.
	Edges        FarmEdges `json:"edges"`
	selectValues sql.SelectValues
}

// FarmEdges holds the relations/edges for other nodes in the graph.
type FarmEdges struct {
	// Owner holds the value of the owner edge.
	Owner *User `json:"owner,omitempty"`
	// Buildings holds the value of the buildings edge.
	Buildings []*Building `json:"buildings,omitempty"`
	// Animals holds the value of the animals edge.
	Animals []*Animal `json:"animals,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// OwnerOrErr returns the Owner value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e FarmEdges) OwnerOrErr() (*User, error) {
	if e.Owner != nil {
		return e.Owner, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "owner"}
}

// BuildingsOrErr returns the Buildings value or an error if the edge
// was not loaded in eager-loading.
func (e FarmEdges) BuildingsOrErr() ([]*Building, error) {
	if e.loadedTypes[1] {
		return e.Buildings, nil
	}
	return nil, &NotLoadedError{edge: "buildings"}
}

// AnimalsOrErr returns the Animals value or an error if the edge
// was not loaded in eager-loading.
func (e FarmEdges) AnimalsOrErr() ([]*Animal, error) {
	if e.loadedTypes[2] {
		return e.Animals, nil
	}
	return nil, &NotLoadedError{edge: "animals"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Farm) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case farm.FieldSpecialty, farm.FieldPlots:
			values[i] = new([]byte)
		case farm.FieldLevel:
			values[i] = new(sql.NullInt64)
		case farm.FieldName:
			values[i] = new(sql.NullString)
		case farm.FieldLastTickAt, farm.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case farm.FieldID, farm.FieldOwnerID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Farm fields.
func (_m *Farm) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case farm.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case farm.FieldOwnerID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field owner_id", values[i])
			} else if value != nil {
				_m.OwnerID = *value
			}
		case farm.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case farm.FieldLevel:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field level", values[i])
			} else if value.Valid {
				_m.Level = int(value.Int64)
			}
		case farm.FieldSpecialty:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field specialty", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Specialty); err != nil {
					return fmt.Errorf("unmarshal field specialty: %w", err)
				}
			}
		case farm.FieldPlots:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field plots", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Plots); err != nil {
					return fmt.Errorf("unmarshal field plots: %w", err)
				}
			}
		case farm.FieldLastTickAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_tick_at", values[i])
			} else if value.Valid {
				_m.LastTickAt = value.Time
			}
		case farm.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Farm.
// This includes values selected through modifiers, order, etc.
func (_m *Farm) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryOwner queries the "owner" edge of the Farm entity.
func (_m *Farm) QueryOwner() *UserQuery {
	return NewFarmClient(_m.config).QueryOwner(_m)
}

// QueryBuildings queries the "buildings" edge of the Farm entity.
func (_m *Farm) QueryBuildings() *BuildingQuery {
	return NewFarmClient(_m.config).QueryBuildings(_m)
}

// QueryAnimals queries the "animals" edge of the Farm entity.
func (_m *Farm) QueryAnimals() *AnimalQuery {
	return NewFarmClient(_m.config).QueryAnimals(_m)
}

// Update returns a builder for updating this Farm.
// Note that you need to call Farm.Unwrap() before calling this method if this Farm
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Farm) Update() *FarmUpdateOne {
	return NewFarmClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Farm entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Farm) Unwrap() *Farm {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Farm is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Farm) String() string {
	var builder strings.Builder
	builder.WriteString("Farm(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("owner_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.OwnerID))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("level=")
	builder.WriteString(fmt.Sprintf("%v", _m.Level))
	builder.WriteString(", ")
	builder.WriteString("specialty=")
	builder.WriteString(fmt.Sprintf("%v", _m.Specialty))
	builder.WriteString(", ")
	builder.WriteString("plots=")
	builder.WriteString(fmt.Sprintf("%v", _m.Plots))
	builder.WriteString(", ")
	builder.WriteString("last_tick_at=")
	builder.WriteString(_m.LastTickAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Farms is a parsable slice of Farm.
type Farms []*Farm
