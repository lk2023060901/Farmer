// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/dailycheckin"
	"github.com/liukai/farmer/server/ent/user"
)

// DailyCheckin is the model entity for the DailyCheckin schema.
type DailyCheckin struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// 签到日期，存为当天 00:00:00 UTC
	CheckinDate time.Time `json:"checkin_date,omitempty"`
	// 连续签到天数
	ConsecutiveDays int `json:"consecutive_days,omitempty"`
	// coins/seed/potion/diamonds 等
	RewardType string `json:"reward_type,omitempty"`
	// RewardQuantity holds the value of the "reward_quantity" field.
	RewardQuantity int `json:"reward_quantity,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the DailyCheckinQuery when eager-loading is set.
	Edges        DailyCheckinEdges `json:"edges"`
	selectValues sql.SelectValues
}

// DailyCheckinEdges holds the relations/edges for other nodes in the graph.
type DailyCheckinEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e DailyCheckinEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DailyCheckin) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dailycheckin.FieldConsecutiveDays, dailycheckin.FieldRewardQuantity:
			values[i] = new(sql.NullInt64)
		case dailycheckin.FieldRewardType:
			values[i] = new(sql.NullString)
		case dailycheckin.FieldCheckinDate, dailycheckin.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case dailycheckin.FieldID, dailycheckin.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DailyCheckin fields.
func (_m *DailyCheckin) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dailycheckin.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case dailycheckin.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				_m.UserID = *value
			}
		case dailycheckin.FieldCheckinDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field checkin_date", values[i])
			} else if value.Valid {
				_m.CheckinDate = value.Time
			}
		case dailycheckin.FieldConsecutiveDays:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field consecutive_days", values[i])
			} else if value.Valid {
				_m.ConsecutiveDays = int(value.Int64)
			}
		case dailycheckin.FieldRewardType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reward_type", values[i])
			} else if value.Valid {
				_m.RewardType = value.String
			}
		case dailycheckin.FieldRewardQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field reward_quantity", values[i])
			} else if value.Valid {
				_m.RewardQuantity = int(value.Int64)
			}
		case dailycheckin.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DailyCheckin.
// This includes values selected through modifiers, order, etc.
func (_m *DailyCheckin) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the DailyCheckin entity.
func (_m *DailyCheckin) QueryUser() *UserQuery {
	return NewDailyCheckinClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this DailyCheckin.
// Note that you need to call DailyCheckin.Unwrap() before calling this method if this DailyCheckin
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DailyCheckin) Update() *DailyCheckinUpdateOne {
	return NewDailyCheckinClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DailyCheckin entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DailyCheckin) Unwrap() *DailyCheckin {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: DailyCheckin is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DailyCheckin) String() string {
	var builder strings.Builder
	builder.WriteString("DailyCheckin(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.UserID))
	builder.WriteString(", ")
	builder.WriteString("checkin_date=")
	builder.WriteString(_m.CheckinDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("consecutive_days=")
	builder.WriteString(fmt.Sprintf("%v", _m.ConsecutiveDays))
	builder.WriteString(", ")
	builder.WriteString("reward_type=")
	builder.WriteString(_m.RewardType)
	builder.WriteString(", ")
	builder.WriteString("reward_quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.RewardQuantity))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// DailyCheckins is a parsable slice of DailyCheckin.
type DailyCheckins []*DailyCheckin
