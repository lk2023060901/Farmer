// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/chatlog"
	"github.com/liukai/farmer/server/ent/predicate"
	"github.com/liukai/farmer/server/ent/user"
)

// ChatLogQuery is the builder for querying ChatLog entities.
type ChatLogQuery struct {
	config
	ctx         *QueryContext
	order       []chatlog.OrderOption
	inters      []Interceptor
	predicates  []predicate.ChatLog
	withUserA   *UserQuery
	withUserB   *UserQuery
	withSpeaker *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ChatLogQuery builder.
func (_q *ChatLogQuery) Where(ps ...predicate.ChatLog) *ChatLogQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *ChatLogQuery) Limit(limit int) *ChatLogQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *ChatLogQuery) Offset(offset int) *ChatLogQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *ChatLogQuery) Unique(unique bool) *ChatLogQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *ChatLogQuery) Order(o ...chatlog.OrderOption) *ChatLogQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryUserA chains the current query on the "user_a" edge.
func (_q *ChatLogQuery) QueryUserA() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(chatlog.Table, chatlog.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, chatlog.UserATable, chatlog.UserAColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUserB chains the current query on the "user_b" edge.
func (_q *ChatLogQuery) QueryUserB() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(chatlog.Table, chatlog.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, chatlog.UserBTable, chatlog.UserBColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySpeaker chains the current query on the "speaker" edge.
func (_q *ChatLogQuery) QuerySpeaker() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(chatlog.Table, chatlog.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, chatlog.SpeakerTable, chatlog.SpeakerColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ChatLog entity from the query.
// Returns a *NotFoundError when no ChatLog was found.
func (_q *ChatLogQuery) First(ctx context.Context) (*ChatLog, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{chatlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *ChatLogQuery) FirstX(ctx context.Context) *ChatLog {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ChatLog ID from the query.
// Returns a *NotFoundError when no ChatLog ID was found.
func (_q *ChatLogQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{chatlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *ChatLogQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ChatLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ChatLog entity is found.
// Returns a *NotFoundError when no ChatLog entities are found.
func (_q *ChatLogQuery) Only(ctx context.Context) (*ChatLog, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{chatlog.Label}
	default:
		return nil, &NotSingularError{chatlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *ChatLogQuery) OnlyX(ctx context.Context) *ChatLog {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ChatLog ID in the query.
// Returns a *NotSingularError when more than one ChatLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *ChatLogQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{chatlog.Label}
	default:
		err = &NotSingularError{chatlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *ChatLogQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ChatLogs.
func (_q *ChatLogQuery) All(ctx context.Context) ([]*ChatLog, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ChatLog, *ChatLogQuery]()
	return withInterceptors[[]*ChatLog](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *ChatLogQuery) AllX(ctx context.Context) []*ChatLog {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ChatLog IDs.
func (_q *ChatLogQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(chatlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *ChatLogQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *ChatLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*ChatLogQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *ChatLogQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *ChatLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *ChatLogQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ChatLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *ChatLogQuery) Clone() *ChatLogQuery {
	if _q == nil {
		return nil
	}
	return &ChatLogQuery{
		config:      _q.config,
		ctx:         _q.ctx.Clone(),
		order:       append([]chatlog.OrderOption{}, _q.order...),
		inters:      append([]Interceptor{}, _q.inters...),
		predicates:  append([]predicate.ChatLog{}, _q.predicates...),
		withUserA:   _q.withUserA.Clone(),
		withUserB:   _q.withUserB.Clone(),
		withSpeaker: _q.withSpeaker.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithUserA tells the query-builder to eager-load the nodes that are connected to
// the "user_a" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChatLogQuery) WithUserA(opts ...func(*UserQuery)) *ChatLogQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUserA = query
	return _q
}

// WithUserB tells the query-builder to eager-load the nodes that are connected to
// the "user_b" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChatLogQuery) WithUserB(opts ...func(*UserQuery)) *ChatLogQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUserB = query
	return _q
}

// WithSpeaker tells the query-builder to eager-load the nodes that are connected to
// the "speaker" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *ChatLogQuery) WithSpeaker(opts ...func(*UserQuery)) *ChatLogQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSpeaker = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserAID uuid.UUID `json:"user_a_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ChatLog.Query().
//		GroupBy(chatlog.FieldUserAID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *ChatLogQuery) GroupBy(field string, fields ...string) *ChatLogGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ChatLogGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = chatlog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserAID uuid.UUID `json:"user_a_id,omitempty"`
//	}
//
//	client.ChatLog.Query().
//		Select(chatlog.FieldUserAID).
//		Scan(ctx, &v)
func (_q *ChatLogQuery) Select(fields ...string) *ChatLogSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &ChatLogSelect{ChatLogQuery: _q}
	sbuild.label = chatlog.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ChatLogSelect configured with the given aggregations.
func (_q *ChatLogQuery) Aggregate(fns ...AggregateFunc) *ChatLogSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *ChatLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !chatlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *ChatLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ChatLog, error) {
	var (
		nodes       = []*ChatLog{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withUserA != nil,
			_q.withUserB != nil,
			_q.withSpeaker != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ChatLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ChatLog{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withUserA; query != nil {
		if err := _q.loadUserA(ctx, query, nodes, nil,
			func(n *ChatLog, e *User) { n.Edges.UserA = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUserB; query != nil {
		if err := _q.loadUserB(ctx, query, nodes, nil,
			func(n *ChatLog, e *User) { n.Edges.UserB = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSpeaker; query != nil {
		if err := _q.loadSpeaker(ctx, query, nodes, nil,
			func(n *ChatLog, e *User) { n.Edges.Speaker = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *ChatLogQuery) loadUserA(ctx context.Context, query *UserQuery, nodes []*ChatLog, init func(*ChatLog), assign func(*ChatLog, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ChatLog)
	for i := range nodes {
		fk := nodes[i].UserAID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_a_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ChatLogQuery) loadUserB(ctx context.Context, query *UserQuery, nodes []*ChatLog, init func(*ChatLog), assign func(*ChatLog, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ChatLog)
	for i := range nodes {
		fk := nodes[i].UserBID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_b_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *ChatLogQuery) loadSpeaker(ctx context.Context, query *UserQuery, nodes []*ChatLog, init func(*ChatLog), assign func(*ChatLog, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ChatLog)
	for i := range nodes {
		fk := nodes[i].SpeakerUserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "speaker_user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *ChatLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *ChatLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(chatlog.Table, chatlog.Columns, sqlgraph.NewFieldSpec(chatlog.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, chatlog.FieldID)
		for i := range fields {
			if fields[i] != chatlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withUserA != nil {
			_spec.Node.AddColumnOnce(chatlog.FieldUserAID)
		}
		if _q.withUserB != nil {
			_spec.Node.AddColumnOnce(chatlog.FieldUserBID)
		}
		if _q.withSpeaker != nil {
			_spec.Node.AddColumnOnce(chatlog.FieldSpeakerUserID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *ChatLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(chatlog.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = chatlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ChatLogGroupBy is the group-by builder for ChatLog entities.
type ChatLogGroupBy struct {
	selector
	build *ChatLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *ChatLogGroupBy) Aggregate(fns ...AggregateFunc) *ChatLogGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *ChatLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChatLogQuery, *ChatLogGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *ChatLogGroupBy) sqlScan(ctx context.Context, root *ChatLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ChatLogSelect is the builder for selecting fields of ChatLog entities.
type ChatLogSelect struct {
	*ChatLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *ChatLogSelect) Aggregate(fns ...AggregateFunc) *ChatLogSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *ChatLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ChatLogQuery, *ChatLogSelect](ctx, _s.ChatLogQuery, _s, _s.inters, v)
}

func (_s *ChatLogSelect) sqlScan(ctx context.Context, root *ChatLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
