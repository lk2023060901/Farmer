// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/helprequest"
	"github.com/liukai/farmer/server/ent/user"
)

// HelpRequest is the model entity for the HelpRequest schema.
type HelpRequest struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 求助方
	RequesterID uuid.UUID `json:"requester_id,omitempty"`
	// 被求助方
	HelperID uuid.UUID `json:"helper_id,omitempty"`
	// seed/material/coins 等
	ResourceType string `json:"resource_type,omitempty"`
	// 物品ID，coins 类型为 null
	ResourceID *string `json:"resource_id,omitempty"`
	// Quantity holds the value of the "quantity" field.
	Quantity int `json:"quantity,omitempty"`
	// Message holds the value of the "message" field.
	Message *string `json:"message,omitempty"`
	// pending/accepted/rejected/expired
	Status string `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// RespondedAt holds the value of the "responded_at" field.
	RespondedAt *time.Time `json:"responded_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the HelpRequestQuery when eager-loading is set.
	Edges        HelpRequestEdges `json:"edges"`
	selectValues sql.SelectValues
}

// HelpRequestEdges holds the relations/edges for other nodes in the graph.
type HelpRequestEdges struct {
	// Requester holds the value of the requester edge.
	Requester *User `json:"requester,omitempty"`
	// Helper holds the value of the helper edge.
	Helper *User `json:"helper,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// RequesterOrErr returns the Requester value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HelpRequestEdges) RequesterOrErr() (*User, error) {
	if e.Requester != nil {
		return e.Requester, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "requester"}
}

// HelperOrErr returns the Helper value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e HelpRequestEdges) HelperOrErr() (*User, error) {
	if e.Helper != nil {
		return e.Helper, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "helper"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HelpRequest) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case helprequest.FieldQuantity:
			values[i] = new(sql.NullInt64)
		case helprequest.FieldResourceType, helprequest.FieldResourceID, helprequest.FieldMessage, helprequest.FieldStatus:
			values[i] = new(sql.NullString)
		case helprequest.FieldCreatedAt, helprequest.FieldRespondedAt:
			values[i] = new(sql.NullTime)
		case helprequest.FieldID, helprequest.FieldRequesterID, helprequest.FieldHelperID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HelpRequest fields.
func (_m *HelpRequest) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case helprequest.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case helprequest.FieldRequesterID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field requester_id", values[i])
			} else if value != nil {
				_m.RequesterID = *value
			}
		case helprequest.FieldHelperID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field helper_id", values[i])
			} else if value != nil {
				_m.HelperID = *value
			}
		case helprequest.FieldResourceType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field resource_type", values[i])
			} else if value.Valid {
				_m.ResourceType = value.String
			}
		case helprequest.FieldResourceID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field resource_id", values[i])
			} else if value.Valid {
				_m.ResourceID = new(string)
				*_m.ResourceID = value.String
			}
		case helprequest.FieldQuantity:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field quantity", values[i])
			} else if value.Valid {
				_m.Quantity = int(value.Int64)
			}
		case helprequest.FieldMessage:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field message", values[i])
			} else if value.Valid {
				_m.Message = new(string)
				*_m.Message = value.String
			}
		case helprequest.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case helprequest.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case helprequest.FieldRespondedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field responded_at", values[i])
			} else if value.Valid {
				_m.RespondedAt = new(time.Time)
				*_m.RespondedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HelpRequest.
// This includes values selected through modifiers, order, etc.
func (_m *HelpRequest) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRequester queries the "requester" edge of the HelpRequest entity.
func (_m *HelpRequest) QueryRequester() *UserQuery {
	return NewHelpRequestClient(_m.config).QueryRequester(_m)
}

// QueryHelper queries the "helper" edge of the HelpRequest entity.
func (_m *HelpRequest) QueryHelper() *UserQuery {
	return NewHelpRequestClient(_m.config).QueryHelper(_m)
}

// Update returns a builder for updating this HelpRequest.
// Note that you need to call HelpRequest.Unwrap() before calling this method if this HelpRequest
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *HelpRequest) Update() *HelpRequestUpdateOne {
	return NewHelpRequestClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the HelpRequest entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *HelpRequest) Unwrap() *HelpRequest {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: HelpRequest is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *HelpRequest) String() string {
	var builder strings.Builder
	builder.WriteString("HelpRequest(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("requester_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.RequesterID))
	builder.WriteString(", ")
	builder.WriteString("helper_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.HelperID))
	builder.WriteString(", ")
	builder.WriteString("resource_type=")
	builder.WriteString(_m.ResourceType)
	builder.WriteString(", ")
	if v := _m.ResourceID; v != nil {
		builder.WriteString("resource_id=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("quantity=")
	builder.WriteString(fmt.Sprintf("%v", _m.Quantity))
	builder.WriteString(", ")
	if v := _m.Message; v != nil {
		builder.WriteString("message=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.RespondedAt; v != nil {
		builder.WriteString("responded_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// HelpRequests is a parsable slice of HelpRequest.
type HelpRequests []*HelpRequest
