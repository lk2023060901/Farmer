// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/liukai/farmer/server/ent/season"
)

// Season is the model entity for the Season schema.
type Season struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// 赛季序号 1, 2, 3...
	Number int `json:"number,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// StartAt holds the value of the "start_at" field.
	StartAt time.Time `json:"start_at,omitempty"`
	// EndAt holds the value of the "end_at" field.
	EndAt time.Time `json:"end_at,omitempty"`
	// upcoming/active/settling/completed
	Status string `json:"status,omitempty"`
	// 赛季任务配置
	TasksConfig []interface{} `json:"tasks_config,omitempty"`
	// 排名奖励配置
	RewardsConfig map[string]interface{} `json:"rewards_config,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the SeasonQuery when eager-loading is set.
	Edges        SeasonEdges `json:"edges"`
	selectValues sql.SelectValues
}

// SeasonEdges holds the relations/edges for other nodes in the graph.
type SeasonEdges struct {
	// Scores holds the value of the scores edge.
	Scores []*SeasonScore `json:"scores,omitempty"`
	// TaskProgresses holds the value of the task_progresses edge.
	TaskProgresses []*SeasonTaskProgress `json:"task_progresses,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ScoresOrErr returns the Scores value or an error if the edge
// was not loaded in eager-loading.
func (e SeasonEdges) ScoresOrErr() ([]*SeasonScore, error) {
	if e.loadedTypes[0] {
		return e.Scores, nil
	}
	return nil, &NotLoadedError{edge: "scores"}
}

// TaskProgressesOrErr returns the TaskProgresses value or an error if the edge
// was not loaded in eager-loading.
func (e SeasonEdges) TaskProgressesOrErr() ([]*SeasonTaskProgress, error) {
	if e.loadedTypes[1] {
		return e.TaskProgresses, nil
	}
	return nil, &NotLoadedError{edge: "task_progresses"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Season) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case season.FieldTasksConfig, season.FieldRewardsConfig:
			values[i] = new([]byte)
		case season.FieldNumber:
			values[i] = new(sql.NullInt64)
		case season.FieldName, season.FieldStatus:
			values[i] = new(sql.NullString)
		case season.FieldStartAt, season.FieldEndAt:
			values[i] = new(sql.NullTime)
		case season.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Season fields.
func (_m *Season) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case season.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case season.FieldNumber:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field number", values[i])
			} else if value.Valid {
				_m.Number = int(value.Int64)
			}
		case season.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case season.FieldStartAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_at", values[i])
			} else if value.Valid {
				_m.StartAt = value.Time
			}
		case season.FieldEndAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field end_at", values[i])
			} else if value.Valid {
				_m.EndAt = value.Time
			}
		case season.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case season.FieldTasksConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field tasks_config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.TasksConfig); err != nil {
					return fmt.Errorf("unmarshal field tasks_config: %w", err)
				}
			}
		case season.FieldRewardsConfig:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field rewards_config", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.RewardsConfig); err != nil {
					return fmt.Errorf("unmarshal field rewards_config: %w", err)
				}
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Season.
// This includes values selected through modifiers, order, etc.
func (_m *Season) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryScores queries the "scores" edge of the Season entity.
func (_m *Season) QueryScores() *SeasonScoreQuery {
	return NewSeasonClient(_m.config).QueryScores(_m)
}

// QueryTaskProgresses queries the "task_progresses" edge of the Season entity.
func (_m *Season) QueryTaskProgresses() *SeasonTaskProgressQuery {
	return NewSeasonClient(_m.config).QueryTaskProgresses(_m)
}

// Update returns a builder for updating this Season.
// Note that you need to call Season.Unwrap() before calling this method if this Season
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Season) Update() *SeasonUpdateOne {
	return NewSeasonClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Season entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Season) Unwrap() *Season {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Season is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Season) String() string {
	var builder strings.Builder
	builder.WriteString("Season(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("number=")
	builder.WriteString(fmt.Sprintf("%v", _m.Number))
	builder.WriteString(", ")
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("start_at=")
	builder.WriteString(_m.StartAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("end_at=")
	builder.WriteString(_m.EndAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("tasks_config=")
	builder.WriteString(fmt.Sprintf("%v", _m.TasksConfig))
	builder.WriteString(", ")
	builder.WriteString("rewards_config=")
	builder.WriteString(fmt.Sprintf("%v", _m.RewardsConfig))
	builder.WriteByte(')')
	return builder.String()
}

// Seasons is a parsable slice of Season.
type Seasons []*Season
